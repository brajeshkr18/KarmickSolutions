@model PagedList.IPagedList<DemoModel.CartModel.PlacedOrderViewModel>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Manage Order";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    List<DemoModel.CartModel.OrderDetailViewModel> OrderDet = new List<DemoModel.CartModel.OrderDetailViewModel>();
}
<link href="~/AdminAsset/assets/libs/datatables.net-bs4/css/dataTables.bootstrap4.css" rel="stylesheet">
<div class="page-wrapper">
    <!-- ============================================================== -->
    <!-- Bread crumb and right sidebar toggle -->
    <!-- ============================================================== -->
    <div class="page-breadcrumb">
        <div class="row">
            <div class="col-12 d-flex no-block align-items-center">
                <h4 class="page-title">Tables</h4>
                <div class="ml-auto text-right">
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="#">Home</a></li>
                            <li class="breadcrumb-item active" aria-current="page">Library</li>
                        </ol>
                    </nav>
                </div>
            </div>
        </div>
    </div>
    <!-- ============================================================== -->
    <!-- End Bread crumb and right sidebar toggle -->
    <!-- ============================================================== -->
    <!-- ============================================================== -->
    <!-- Container fluid  -->
    <!-- ============================================================== -->
    <div class="container-fluid">
        <!-- ============================================================== -->
        <!-- Start Page Content -->
        <!-- ============================================================== -->
        <div class="row">
            <div class="col-12">

                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Manage Order</h5>

                        <div class="table-responsive">
                            @{Html.RenderPartial("_Orders", Model);}
                        </div>
                        @*<div class="col-lg-5 col-md-5 col-sm-5 col-xs-12">
                        <div style="float:left">
                            <label>
                                Show <select name="lengthSelect" id="lengthSelect" aria-controls="example" class="dropdown">
                                    <option value="10">10</option>
                                    <option value="25">25</option>
                                    <option value="50">50</option>
                                    <option value="100">100</option>
                                    <option value="500">500</option>
                                </select> entries
                            </label>
                        </div>
                    </div>*@
                        <div class="col-lg-7 col-md-7 col-sm-7 col-xs-12">
                            <div class="pagination-margin  paginate_button page-item ">
                                @Html.PagedListPager(Model, page => Url.Action("ManageOrders", new { page, pageSize = ViewBag.PageSize }))
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div id="myPartialContainer" class="col-12"></div>
            @*@{Html.RenderPartial("_ViewOrders", OrderDet);}*@
            
        </div>
        <!-- ============================================================== -->
        <!-- End PAge Content -->
        <!-- ============================================================== -->
        <!-- ============================================================== -->
        <!-- Right sidebar -->
        <!-- ============================================================== -->
        <!-- .right-sidebar -->
        <!-- ============================================================== -->
        <!-- End Right sidebar -->
        <!-- ============================================================== -->
    </div>
    <!-- ============================================================== -->
    <!-- End Container fluid  -->
    <!-- ============================================================== -->
    <!-- ============================================================== -->
    <!-- footer -->
    <!-- ============================================================== -->
    <footer class="footer text-center">
        All Rights Reserved by . Designed and Developed by <a href="http://brajeshkumar.co.in/">Brajesh Kumar</a><a href="http://brajeshkumar.co.in/"></a>.
    </footer>
    <!-- ============================================================== -->
    <!-- End footer -->
    <!-- ============================================================== -->
</div>
<div class="modal fade none-border" id="notImplementedPopUp">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
            </div>
            <div class="modal-body">
                <input type="hidden" id="productId" value="0" />
                <label class="control-label" id="notImplementedMessage">Do you really want to delete the record. ?</label>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-danger waves-effect waves-light save-category" data-dismiss="modal"
                        onclick="DeleteConfirm()">
                    Yes
                </button>
                <button type="button" class="btn btn-secondary waves-effect" data-dismiss="modal">No</button>
            </div>
        </div>
    </div>
</div>
<script src="~/AdminAsset/assets/extra-libs/DataTables/datatables.min.js"></script>
<script>
    function ViewData(shippingId) {

        $.ajax({
            type: "POST",
            data: { ShippingId: shippingId },
            url: "/Order/ViewOrders",
            dataType: 'html',
            success: function (data) {
                $('#myPartialContainer').html(data);
            }

        })
    };
    function OpenPopup() {
        $("#notImplementedMessage").html('Functionality not implement')
        $("#notImplementedPopUp").modal('show');
        //$.ajax({
        //    type: "POST",
        //    data: { ShippingId: shippingId },
        //    url: "/Order/ViewOrders",
        //    dataType: 'html',
        //    success: function (data) {
        //        $('#myPartialContainer').html(data);
        //    }

        //})
    };
</script>